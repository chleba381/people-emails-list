<h1>Upravit osobu <%= @person.name %> <%= @person.surname %></h1>

<div class="list-item">
  <%= render "form", person: @person %>


    <div class="flexbox-container">
      <%= link_to "<< Zpět", :back, class:"btn btn-outline-dark" %>
    </div> 
</div>
